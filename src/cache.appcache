CACHE MANIFEST
# 在此標題下列出的文件將在首次下載後進行緩存。

# 以"#"開頭的是註釋行，但也可滿足其他用途。應用的緩存會在其manifest文件更改時被更新。如果您編輯了一幅圖片，或者修改了一個JavaScript函數，這些改變都不會被重新緩存。更新註釋行中的日期和版本號是一種使瀏覽器重新緩存文件的辦法。
# 2012-02-21 v1.0.0

js/cache.js
img/cache.png

# 在此標題下列出的文件需要與服務器的連接，且不會被緩存。
NETWORK:
# login.asp

# 在此標題下列出的文件規定當頁面無法訪問時的回退頁面（比如404頁面）。第一個URI是資源，第二個是替補。
FALLBACK:
# /html/ /offline.html

# 一旦應用被緩存，它就會保持緩存直到發生下列情況：
# ‧用戶清空瀏覽器緩存
# ‧manifest 文件被修改
# ‧由程序來更新應用緩存
